# Pass-through check prompt (negative control)

This template exists to contrast with selector outputs in Phase 27 demos.

If this prompt is used on a **prompted** Artifact Output, rewrite the upstream text into a single sentence starting with `PROMPTED:`.

If you are reading this file manually, it is not a Skill — it is a prompt template for later Artifact Output projection.

## Upstream artifact

{{upstream}}
